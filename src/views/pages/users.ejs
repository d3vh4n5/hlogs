<section>
    <h2>Usuarios registrados:</h2>

    <ul>
        <!-- Sintaxis JavaScript -->
        <% if(users){ %>
            <% users.forEach(user => { %>
                <!-- Dentro, sintaxis HTML -->
                <li><%= user.name %></li>
        
            <% }) %>
        <% } else {%>

        <% } %>
    </ul>
     
</section>